#include <iostream>
#include <vector>

use namespace std;

vector<int> twoSum (const vector<int>& numbers, const int& target) {

  vector<int> pair;
  
  for (int i=0; i<numbers.size(); i++) {
  }

}
